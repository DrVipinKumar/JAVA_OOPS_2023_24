package iopackage;

import java.io.File;
class ListFile{
	void displayFile(String path) {
		 File dir=new File(path);
	        File [] files=dir.listFiles();
	        System.out.println("\t\tName\t\t\t\tType\t\t\tSize");
	        for(File f:files) {
	        	checkDir(f);
	        }
	}
	void checkDir(File f) {
		if (f.isFile())
		{
			System.out.println(f.getName()+"\t\t\tFile\t\t\t"+f.length()/(1024*1024)+"MB");
		} else {
			System.out.println(f.getName()+"\t\t\tDirectory\t\t\t"+f.length()/(1024*1024)+"MB");
		}
	}
}
public class MyDir {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
       new ListFile().displayFile("src");
	}

}
